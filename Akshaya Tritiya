<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>AR Newspaper Experience</title>

  <!-- MindAR + A-Frame -->
  <script src="https://cdn.jsdelivr.net/npm/mind-ar@1.2.2/dist/mindar-image.prod.js"></script>
  <script src="https://aframe.io/releases/1.4.0/aframe.min.js"></script>

  <style>
    body {
      margin: 0;
      overflow: hidden;
      font-family: sans-serif;
    }

    .instructions {
      position: fixed;
      bottom: 20px;
      width: 100%;
      text-align: center;
      color: white;
      font-size: 14px;
      z-index: 10;
    }
  </style>
</head>

<body>

  <!-- Instruction Text -->
  <div class="instructions">
    Scan the QR → Point camera at the newspaper
  </div>

  <!-- AR Scene -->
  <a-scene
    mindar-image="imageTargetSrc: target.mind; autoStart: true;"
    vr-mode-ui="enabled: false"
    device-orientation-permission-ui="enabled: true"
  >

    <!-- Assets -->
    <a-assets>
      <video
        id="vid"
        src="https://res.cloudinary.com/YOUR-ID/video/upload/YOUR-VIDEO.mp4"
        loop
        muted
        playsinline
        webkit-playsinline
        crossorigin="anonymous"
      ></video>
    </a-assets>

    <!-- Camera -->
    <a-camera position="0 0 0" look-controls="enabled: false"></a-camera>

    <!-- Image Target -->
    <a-entity mindar-image-target="targetIndex: 0">

      <!-- Video Overlay -->
      <a-video
        src="#vid"
        width="1.8"
        height="1"
        position="0 0.3 0"
      ></a-video>

    </a-entity>

  </a-scene>

  <script>
    const video = document.querySelector("#vid");
    const scene = document.querySelector("a-scene");

    scene.addEventListener("targetFound", () => {
      video.play();
    });

    scene.addEventListener("targetLost", () => {
      video.pause();
    });
  </script>

</body>
</html>
